services:

  spring_boot:
    build:
      context: .
      dockerfile: Dockerfile
    image: kimeunsur/birthday:latest
    container_name: birthday_prod
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: always
